import classes from "./AppointmentButton.module.css";

const AppointmentButton = function () {
  return <button className={classes.btn}>Book Appointment</button>;
};

export default AppointmentButton;
